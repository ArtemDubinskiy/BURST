# 🔥  B.U.R.S.T.  

[![Typing SVG](https://readme-typing-svg.demolab.com?font=Fira+Code&weight=600&size=40&duration=6000&pause=1000&color=EB13F7&width=500&height=60&lines=B.U.R.S.T.)](https://git.io/typing-svg)

![Platform](https://img.shields.io/badge/platform-Windows%20%7C%20.NET-blue)
![License](https://img.shields.io/badge/license-MIT-green)
![Build](https://img.shields.io/badge/build-passing-brightgreen)
### Brutal Utilization & Resilience Stress Testing  
Высокопроизводительная утилита стресс-тестирования CPU под Windows (.NET), созданная для обеспечения экстремальной нагрузки на вычислительные блоки процессоров.

---

## 🎯 Назначение

BURST создает нагрузку на:
- Целочисленный ALU  
- FPU  
- SSE / AVX / AVX2 (+ gather)  
- Подсистему памяти  
- Кэш-иерархию  
- Криптографию (SHA-256/512)

Одновременно утилита:
- Отображает консольный монитор с прогрессом  
- Ведёт логирование в **JSONL**  
- Может работать в CLI, интерактивном и гибридном режимах  
- Легко расширяется новыми тестами  

---

# 📌 Набор тестов

### ✔ Типы тестов
| ID | Тип нагрузки |
|----|--------------|
| 1 | Целочисленные операции |
| 2 | Плавающая точка |
| 3 | Память |
| 4 | SSE |
| 5 | AVX |
| 6 | AVX2 (+ gather) |
| 7 | Хеширование SHA-256/512 |
| 8 | Игровой смешанный тест |

---

### ✔ Порядок выполнения тестов
| Режим | Описание |
|-------|----------|
| `seq` | Последовательно, тест за тестом |
| `round` | Round-Robin — по одному циклу каждого теста |
| `rand` | Случайный выбор теста (пока есть циклы) |

---

### ✔ Другие функции
- Обнаружение вычислительных ошибок  
- Переключение потоков на конкретные ядра (thread affinity)  
- Монитор CPU (1 Гц)  
- Логирование в `log.jsonl`  
- Свободное комбинирование тестов и циклов  

---

# 🖥 Работа с программой

## 📚 Основные команды CLI

`BURST.exe --help`

`BURST.exe --list-tests`

`BURST.exe --cores all --tests 1,2,3,4 --cycles 100,100,100,100 --mode round`

---

# 🔧 Аргументы запуска

## 🎛️ Ядра: `--cores`
| Формат | Пример | Значение |
|--------|--------|----------|
| `all` | all | Все ядра |
| `even` | even | Чётные ядра |
| `odd` | odd | Нечётные ядра |
| Список | 0,1,2,9 | Ядра вручную |
| Диапазон | 0-7 | С 0 до 7 |
| Комбинация | 0-3,6,8-10 | Несколько диапазонов |

---

## 🧪 Тесты: `--tests`
Указываются ID тестов, например:

`--tests 1,4,6`


---

## 🔁 Циклы: `--cycles`
Количество выполнений каждого теста.  
Длина списка = количество тестов.

| Пример | Значение |
|--------|----------|
| `--cycles 100,200` | Первый тест — 100 циклов, второй — 200 |
| `--cycles 100,0` | Второй тест пропущен |

---

## 🔄 Режим выполнения: `--mode`
| Режим | Что делает |
|--------|-------------|
| `seq` | Выполняет тесты по порядку |
| `round` | По одному циклу каждого теста |
| `rand` | Случайный выбор |

---

# 🧩 Гибридный режим
Если указаны не все параметры, программа автоматически запросит недостающие.

**Пример**

`BURST.exe --cores 0-3`


BURST спросит:
- список тестов  
- количество циклов  
- режим выполнения  

---

# 🚀 Примеры использования

### 1. Максимальная AVX/AVX2-нагрузка

`BURST.exe --cores all --tests 5,6 --cycles 300,600 --mode seq`


### 2. Конкурентная смешанная нагрузка

`BURST.exe --cores even --tests 1,2,4,6 --cycles 100,100,50,200 --mode round`


### 3. Случайный режим с пропусками

`BURST.exe --cores 0-7 --tests 1,2,3,6 --cycles 0,50,0,500 --mode rand`


---

# ⚠ Важное

При нагрузке **всех** потоков особенно тяжёлыми тестами (AVX/FPU/AVX2):

UI-поток может испытывать starvation:
- индикаторы замирают  
- ввод реагирует с задержкой  
- создаётся впечатление зависания  

Это **нормально** — тесты продолжаются выполнять без ошибок.

**Совет:**  
Если нужна отзывчивость — оставьте одно ядро свободным.  
Например, на 8-ядерной системе:
--cores 0-6


---

# 📄 Логирование
Логи записываются в формате JSON Lines:

log.jsonl
{"timestamp":"...","core":2,"event":"cycle","test":"AVX2", ... }


---

# 🧩 Расширяемость
Добавление новых тестов производится через регистрацию нового стресс-модуля, совместимого с интерфейсами ядра BURST.

---
